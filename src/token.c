#include "cc.h"

struct token tok;

void token_arr_init(struct token_arr *array) {
        // TODO: make function that initializes array
}

void token_arr_add(struct token_arr *array) {
        // TODO: make function that adds token to array
}

void token_arr_free(struct token_arr *array) {
        // TODO: make function that frees array
}